name: Stock Checker

on:
  schedule:
    - cron: "0 8-16 * * 1-5"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Obnov stav alertů
        uses: actions/cache@v4
        with:
          path: alert_state.json
          key: alert-state

      - name: Nainstaluj závislosti
        run: pip install -r requirements.txt

      - name: Zkontroluj ceny
        run: python stock_reminder.py
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DI